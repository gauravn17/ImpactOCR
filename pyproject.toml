[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "impactor"
version = "0.1.0"
description = "Offline OCR-based MCQ assessment platform"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
